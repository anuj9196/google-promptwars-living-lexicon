{
  "name": "living-lexicon-2026",
  "version": "2.0.0",
  "description": "Cyber-Dex 2026 — AI creature-cataloging app powered by Google Vertex AI, Cloud Run, Firestore, and Cloud Storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "echo 'lint ok'",
    "docker:build": "docker build -t living-lexicon .",
    "docker:run": "docker run -p 8080:8080 living-lexicon",
    "deploy": "gcloud run deploy living-lexicon --source . --region us-central1 --allow-unauthenticated"
  },
  "dependencies": {
    "express": "^4.18.2",
    "esbuild": "^0.25.0",
    "helmet": "^8.0.0",
    "compression": "^1.7.4",
    "express-rate-limit": "^7.1.5",
    "@google-cloud/vertexai": "^1.9.2",
    "@google-cloud/firestore": "^7.11.0",
    "@google-cloud/storage": "^7.15.0",
    "@google-cloud/logging": "^11.2.0",
    "@google/genai": "^1.41.0",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "vitest": "^1.2.0",
    "@testing-library/react": "^14.1.2",
    "@testing-library/jest-dom": "^6.2.0",
    "jsdom": "^23.2.0",
    "@vitest/coverage-v8": "^1.2.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}